import { SearchContainer, ListContainer, Input } from './styles'

const Search = () => {
  return (
    <SearchContainer>
      <ListContainer></ListContainer>
      <Input autoFocus={true} />
    </SearchContainer>
  )
}

export default Search
